import {createUser, login, logout, logoutFromDevice} from './users';
import {listMyPosts,createPost, deletePost, updatePost, listMyDeletedPosts, listPostsByDevice, listAllPosts} from './posts'
export  {
    createUser, login, logout, logoutFromDevice,
    listMyPosts, listAllPosts,listPostsByDevice, listMyDeletedPosts,
     createPost, deletePost, updatePost,
};